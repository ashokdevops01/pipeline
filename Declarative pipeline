pipeline {
    agent any
   
    stages {
        stage ('Initialize') {
            steps {
                git 'https://github.com/ashokdevops01/maven.git'
    
            }
        }

        stage ('Build') {
            steps {
                sh 'mvn package -f web/pom.xml'
            }
        }
    }
}
